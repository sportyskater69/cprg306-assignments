'use client';
import NewItem from "./NewItem";

export default function Page() {
    return (
        <main className="bg-black flex flex-col items-center h-screen">
            <NewItem />
        </main>
    );
}